// FALLBACK display prices only — the paywall shows the store's localized
// priceString from RevenueCat offerings (PremiumContext.prices) whenever it
// resolves; these USD strings appear only if offerings never load (e.g.
// offline). Keep in sync with App Store Connect / Play Console pricing.
export const PREMIUM_PRICE = '$4.99';
export const REMOVE_ADS_PRICE = '$1.99';
export const CUSTOMIZATION_PRICE = '$3.99';
